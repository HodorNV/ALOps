```yaml
    - task: ALOpsAICodeReview@3
      displayName: 'ALOps AI Code Review'
      inputs:
        ai_provider: azure-openai             # Select the AI provider to use for code review. Supported providers: Azure OpenAI, OpenAI, Anthropic (Claude), GitHub Models. $(ai_provider) $(ai_provider) $(ai_provider)
        ai_api_key:                           # Use a pipeline secret variable. $(ai_api_key) $(ai_api_key) $(ai_api_key)
        ai_base_url:                          # Azure OpenAI only. E.g. https://resource.openai.azure.com $(ai_base_url) $(ai_base_url) $(ai_base_url)
        ai_deployment:                        # Azure OpenAI only. E.g. gpt-4o $(ai_deployment) $(ai_deployment) $(ai_deployment)
        ai_api_version: 2024-08-01-preview    # Azure OpenAI only. The REST API version to use. E.g. 2024-08-01-preview $(ai_api_version) $(ai_api_version) $(ai_api_version)
        ai_model:                             # OpenAI / Anthropic only. E.g. claude-opus-4-5 $(ai_model) $(ai_model) $(ai_model)
        ai_max_tokens: 4096                   # Maximum number of tokens in the AI response per file review. $(ai_max_tokens) $(ai_max_tokens) $(ai_max_tokens)
        ai_max_input_tokens: 120000           # Maximum number of tokens sent to the AI per request. Reduce if hitting context window limits. $(ai_max_input_tokens) $(ai_max_input_tokens) $(ai_max_input_tokens)
        ai_timeout_sec: 120                   # Timeout in seconds for each AI API request. $(ai_timeout_sec) $(ai_timeout_sec) $(ai_timeout_sec)
        include_few_shot_examples: false      # Include example reviews in the prompt to guide the AI output format. Improves consistency but increases token usage. $(include_few_shot_examples) $(include_few_shot_examples) $(include_few_shot_examples)
        ai_fallback_provider: none            # Optional fallback AI provider used when the primary provider fails or is rate-limited. $(ai_fallback_provider) $(ai_fallback_provider) $(ai_fallback_provider)
        ai_fallback_api_key:                  # Use a pipeline secret variable. $(ai_fallback_api_key) $(ai_fallback_api_key) $(ai_fallback_api_key)
        ai_fallback_base_url:                 # Azure OpenAI only. E.g. https://resource.openai.azure.com $(ai_fallback_base_url) $(ai_fallback_base_url) $(ai_fallback_base_url)
        ai_fallback_deployment:               # Azure OpenAI only. E.g. gpt-4o $(ai_fallback_deployment) $(ai_fallback_deployment) $(ai_fallback_deployment)
        ai_fallback_model:                    # OpenAI / Anthropic only. E.g. claude-opus-4-5 $(ai_fallback_model) $(ai_fallback_model) $(ai_fallback_model)
        ai_fallback_api_version:              # Azure OpenAI only. Leave blank to inherit the primary API version. $(ai_fallback_api_version) $(ai_fallback_api_version) $(ai_fallback_api_version)
        base_branch: origin/main              # The base branch to compare against when computing the diff. E.g. origin/main $(base_branch) $(base_branch) $(base_branch)
        file_extensions: .al,.yml,.ps1,.json  # Comma-separated list of file extensions to include in the review. E.g. .al,.yml,.ps1,.json $(file_extensions) $(file_extensions) $(file_extensions)
        rules_path:                           # Path to a custom rules file or directory. Leave blank to use the built-in default rules. $(rules_path) $(rules_path) $(rules_path)
        diff_context_lines: 10                # Number of surrounding unchanged lines to include around each diff hunk for context. $(diff_context_lines) $(diff_context_lines) $(diff_context_lines)
        max_files: 20                         # Maximum number of changed files to review per pipeline run. $(max_files) $(max_files) $(max_files)
        fail_on_severity: error               # Minimum severity level that causes the pipeline task to fail. Use 'Never' to post annotations without failing. $(fail_on_severity) $(fail_on_severity) $(fail_on_severity)
        max_cost_usd: 0                       # Maximum allowed estimated cost in USD per run. The task fails if exceeded. Set to 0 to disable the cost limit. $(max_cost_usd) $(max_cost_usd) $(max_cost_usd)
        post_pr_comments: false               # When enabled, review findings are posted as inline comments on the active pull request. $(post_pr_comments) $(post_pr_comments) $(post_pr_comments)
        pr_comment_dedup: true                # Skip posting a comment if an identical comment already exists on the pull request. $(pr_comment_dedup) $(pr_comment_dedup) $(pr_comment_dedup)
        resolve_fixed_threads: true           # Automatically resolve previously posted review threads when the underlying issue is no longer present. $(resolve_fixed_threads) $(resolve_fixed_threads) $(resolve_fixed_threads)
        post_summary_comment: true            # Post a summary comment on the pull request with overall review statistics and findings. $(post_summary_comment) $(post_summary_comment) $(post_summary_comment)
        review_parallelism: 1                 # Number of files to review in parallel. Increase to speed up reviews, but be mindful of API rate limits. $(review_parallelism) $(review_parallelism) $(review_parallelism)
        requests_per_minute: 60               # Maximum number of AI API requests per minute. Align with your provider quota to avoid rate-limit errors. $(requests_per_minute) $(requests_per_minute) $(requests_per_minute)
        rate_limit_wait_seconds: 60           # Number of seconds to wait before retrying after receiving a rate-limit (HTTP 429) response. $(rate_limit_wait_seconds) $(rate_limit_wait_seconds) $(rate_limit_wait_seconds)
        circuit_breaker_threshold: 3          # Number of consecutive API failures before the circuit breaker opens and halts further requests. $(circuit_breaker_threshold) $(circuit_breaker_threshold) $(circuit_breaker_threshold)
        circuit_breaker_recovery_sec: 120     # Number of seconds to wait after the circuit breaker opens before allowing requests to resume. $(circuit_breaker_recovery_sec) $(circuit_breaker_recovery_sec) $(circuit_breaker_recovery_sec)
        enable_cache: true                    # Cache AI responses to avoid re-reviewing unchanged file content on subsequent pipeline runs. $(enable_cache) $(enable_cache) $(enable_cache)
        cache_ttl_hours: 168                  # Number of hours a cached response remains valid. Default is 168 hours (7 days). $(cache_ttl_hours) $(cache_ttl_hours) $(cache_ttl_hours)
        invalidate_cache: false               # Force all cached entries to be ignored and refreshed on this run. $(invalidate_cache) $(invalidate_cache) $(invalidate_cache)
        cross_run_dedup: false                # Skip review of files whose content was already reviewed and passed in a previous pipeline run. $(cross_run_dedup) $(cross_run_dedup) $(cross_run_dedup)
```

