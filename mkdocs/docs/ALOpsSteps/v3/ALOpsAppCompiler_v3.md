```yaml
    - task: ALOpsAppCompiler@3
      displayName: 'ALOps App Compiler V3'
      inputs:
        altool_package_version: 17.0.30.49729-beta# Version of the `microsoft.dynamics.businesscentral.development.tools` NuGet package to use as compiler tooling. $(altool_package_version) $(altool_package_version) $(altool_package_version)
        altool_cache_hours: 0                 # Number of hours to cache the downloaded NuGet package on the agent. Set to 0 to always download fresh. $(altool_cache_hours) $(altool_cache_hours) $(altool_cache_hours)
        compilation_mode: Serial              # Serial: compiles one app at a time using `altool compile`. Parallel: compiles all apps concurrently using `altool workspace compile`. $(compilation_mode) $(compilation_mode) $(compilation_mode)
        alsourcepath: $(System.DefaultWorkingDirectory)# Root folder to search for app.json files to compile. $(alsourcepath) $(alsourcepath) $(alsourcepath)
        alcachepath:                          # Folder containing symbol .app files (platform + localisation) used as the package cache for compilation. Leave empty to use the AL source path. $(alcachepath) $(alcachepath) $(alcachepath)
        appversiontemplate: 1.0.*.0           # Version template. Use `*` for build number, `?` to keep original part, `A` to use Application version part. Example: `1.0.*.0` $(appversiontemplate) $(appversiontemplate) $(appversiontemplate)
        updatebuildnumber: true               # Update the Azure DevOps build number with the compiled app version. $(updatebuildnumber) $(updatebuildnumber) $(updatebuildnumber)
        appfilenametemplate: %APP_PUBLISHER%_%APP_NAME%_%APP_VERSION%_%BC_VERSION%_%BC_COUNTRY%.app# Template for the output .app filename. Supported tokens: `%APP_PUBLISHER%`, `%APP_NAME%`, `%APP_VERSION%`, `%BC_COUNTRY%`, `%BC_VERSION%`. $(appfilenametemplate) $(appfilenametemplate) $(appfilenametemplate)
        alcodeanalyzer:                       # Comma-separated list of analyzers: `CodeCop`, `UICop`, `AppSourceCop`, `PTECop`, or DLL paths. Used in serial mode only. $(alcodeanalyzer) $(alcodeanalyzer) $(alcodeanalyzer)
        analyzers:                            # Comma-separated analyzers for parallel workspace compile: `CodeCop`, `UICop`, `AppSourceCop`, `PTECop`, `None`. Used in parallel mode only. $(analyzers) $(analyzers) $(analyzers)
        ruleset:                              # Path or URL to a ruleset (.json) file. Overrides VSCode settings. $(ruleset) $(ruleset) $(ruleset)
        suppresswarnings: KEEP                # Override `suppressWarnings` in app.json. Use `KEEP` to preserve existing value, `NONE` to remove, or a comma-separated list of codes. $(suppresswarnings) $(suppresswarnings) $(suppresswarnings)
        ignorepragmas:                        # Comma-separated diagnostic codes to downgrade from warning/error to info (requires `reportsuppresseddiagnostics`). $(ignorepragmas) $(ignorepragmas) $(ignorepragmas)
        showmycode: Keep                      # Override the `showMyCode` property in app.json. $(showmycode) $(showmycode) $(showmycode)
        resourceexposurepolicy_allowdebugging: Keep# Override `resourceExposurePolicy.allowDebugging` in app.json. $(resourceexposurepolicy_allowdebugging) $(resourceexposurepolicy_allowdebugging) $(resourceexposurepolicy_allowdebugging)
        resourceexposurepolicy_allowdownloadingsource: Keep# Override `resourceExposurePolicy.allowDownloadingSource` in app.json. $(resourceexposurepolicy_allowdownloadingsource) $(resourceexposurepolicy_allowdownloadingsource) $(resourceexposurepolicy_allowdownloadingsource)
        resourceexposurepolicy_includesourceinsymbolfile: Keep# Override `resourceExposurePolicy.includeSourceInSymbolFile` in app.json. $(resourceexposurepolicy_includesourceinsymbolfile) $(resourceexposurepolicy_includesourceinsymbolfile) $(resourceexposurepolicy_includesourceinsymbolfile)
        internalsvisibleto: Keep              # Keep or remove the `internalsVisibleTo` array from app.json. $(internalsvisibleto) $(internalsvisibleto) $(internalsvisibleto)
        preprocessorsymbols:                  # Override `preprocessorSymbols` in app.json. Use `NONE` to remove. Comma-separated list. $(preprocessorsymbols) $(preprocessorsymbols) $(preprocessorsymbols)
        applicationinsightskey:               # Override `applicationInsightsKey` or `applicationInsightsConnectionString` in app.json. Use `NONE` to remove. $(applicationinsightskey) $(applicationinsightskey) $(applicationinsightskey)
        publishartifact: true                 # Upload the compiled .app file as a build artifact. $(publishartifact) $(publishartifact) $(publishartifact)
        publishxlif: false                    # Upload the generated .g.xlf translation file as a build artifact. $(publishxlif) $(publishxlif) $(publishxlif)
        failonwarnings: false                 # Fail the task if the compiler produces any warnings. $(failonwarnings) $(failonwarnings) $(failonwarnings)
        failonany: false                      # Fail the task if the compiler produces any diagnostic message (including info). $(failonany) $(failonany) $(failonany)
        printappmanifest: true                # Print the final app.json content to the build log before compilation. $(printappmanifest) $(printappmanifest) $(printappmanifest)
        outputalclogs: true                   # Write the raw altool/alc compiler output to the build log. $(outputalclogs) $(outputalclogs) $(outputalclogs)
        enable_external_rulesets: false       # Pass `/enableexternalrulesets` to alc.exe (serial mode only). $(enable_external_rulesets) $(enable_external_rulesets) $(enable_external_rulesets)
        generatereportlayouts: true           # Enable or disable report layout generation during compilation. $(generatereportlayouts) $(generatereportlayouts) $(generatereportlayouts)
        track_source_build_metadata: true     # Inject build/source metadata (commit, repo URL, build URL) into app.json before compilation (requires runtime >= 12). $(track_source_build_metadata) $(track_source_build_metadata) $(track_source_build_metadata)
        alc_continuebuildonerror: false       # Pass `/continuebuildonerror+` to alc.exe. Serial mode only. $(alc_continuebuildonerror) $(alc_continuebuildonerror) $(alc_continuebuildonerror)
        alc_errorlog: false                   # Pass `/errorlog` to alc.exe and publish the log as a build artifact. Serial mode only. $(alc_errorlog) $(alc_errorlog) $(alc_errorlog)
        updateruntime:                        # Override the `runtime` field in app.json before compilation. $(updateruntime) $(updateruntime) $(updateruntime)
        allowed_publisher_names:              # Comma-separated list of allowed publisher names. Apps with unlisted publishers will cause the task to fail. $(allowed_publisher_names) $(allowed_publisher_names) $(allowed_publisher_names)
        allowed_publisher_names_separator: ,  # Separator character used in the Allowed Publisher Names list. $(allowed_publisher_names_separator) $(allowed_publisher_names_separator) $(allowed_publisher_names_separator)
        additionalprobingpaths:               # Additional paths (one per line) to probe for .NET assemblies referenced from AL. $(additionalprobingpaths) $(additionalprobingpaths) $(additionalprobingpaths)
        maxcpucount:                          # Maximum number of concurrent compilations in parallel mode. Defaults to the number of processors. Has no effect in serial mode. $(maxcpucount) $(maxcpucount) $(maxcpucount)
        sastoken:                             # SAS token used for masking in the pipeline log. $(sastoken) $(sastoken) $(sastoken)
        auto_resolve_ms_symbols: true         # When enabled, scans all app.json dependency lists for Microsoft-published apps, checks the symbol cache, and automatically downloads any missing symbols from the MSSymbols NuGet feed before compilation. $(auto_resolve_ms_symbols) $(auto_resolve_ms_symbols) $(auto_resolve_ms_symbols)
        bc_localization: W1                   # Business Central country/localisation code used to select the correct MSSymbols NuGet package (e.g. `W1`, `BE`, `NL`, `US`, `GB`). Determines the entry-point package: `Microsoft.Application.<country>.symbols`. Defaults to `W1`. $(bc_localization) $(bc_localization) $(bc_localization)
        bc_version:                           # Business Central version filter in Major.Minor format, e.g. `27.4`. When empty the version is auto-detected from the `application` field in app.json (e.g. `"application": "27.4.0.0"` â†’ `27.4`). $(bc_version) $(bc_version) $(bc_version)
        ms_symbols_feed: https://dynamicssmb2.pkgs.visualstudio.com/DynamicsBCPublicFeeds/_packaging/MSSymbols/nuget/v3/index.json# NuGet v3 service index URL for the Microsoft MSSymbols feed. The default points to the public MSSymbols feed which requires no authentication. $(ms_symbols_feed) $(ms_symbols_feed) $(ms_symbols_feed)
        ms_symbols_pat:                       # Optional Personal Access Token for authenticated access to the MSSymbols feed. Leave empty when using the default public feed. $(ms_symbols_pat) $(ms_symbols_pat) $(ms_symbols_pat)
        pwsh: True                            # Run task in Powershell Core. $(pwsh) $(pwsh) $(pwsh)
```

